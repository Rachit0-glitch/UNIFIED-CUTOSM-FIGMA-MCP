{
  "name": "figma-unified-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "Unified MCP coordinator for Plumb and Custom Figma MCP backends.",
  "type": "module",
  "bin": {
    "figma-unified-mcp": "src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check scripts/unified-probe.mjs && node --check scripts/unified-live-sequence.mjs && node --check scripts/custom-mcp-sdk-probe.mjs",
    "test": "node --test \"./tests/*.test.js\"",
    "integration:status": "node scripts/unified-probe.mjs unified_status",
    "integration:plumb": "node scripts/unified-probe.mjs unified_probe_backend '{\"backend\":\"plumb\"}'",
    "integration:custom": "node scripts/unified-probe.mjs unified_probe_backend '{\"backend\":\"custom\"}'"
  },
  "engines": {
    "node": ">=20"
  }
}

